#include<bits/stdc++.h>
using namespace std;
#define ll long long 
int  mod = 1e9+7;
int main()
{
    ll n;
    cin>>n;
    ll sum=1;
    for(ll i=1;i<=n;i++)
    {
        sum=(sum*2)%mod;
    }
    cout<<sum<<endl;
}
